# MISSION.md - builder-a

## Night Shift Mission

Goal: turn the Feishu todo/menu design into the first runnable implementation step.

Priority order:
1. Turn `FEISHU_TODO_HANDLER_SPEC.md` into the first concrete parser/router implementation step.
2. Wire the first file-mutation path for explicit `记录待办` capture into `TODO_INBOX.md`.
3. If the exact runtime entrypoint is still missing, produce the smallest executable scaffold, patch plan, or adapter seam so the next builder can implement directly.

Rules:
- Prefer code, parser scaffolding, fixtures, or exact file-level implementation plans over more product prose.
- Keep changes narrow and verifiable.
- Update `STATUS.md` every run with completed work, blocker, and next step.
