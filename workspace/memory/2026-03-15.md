
## 2026-03-15 agent-reach and queue state
- User wants `English-study / Label Studio` next-pass cleanup to wait until `agent-reach` detection is finished; after detection completed, that gate is cleared and the next pass may open when it becomes top priority.
- User approved continuing host-side `agent-reach` recovery in a conservative mode: do not risk the main OpenClaw runtime, self-service whatever can be safely wired up, and stop for user help only on auth/proxy/cookie or other manual-authorization requirements.
- Host-side `agent-reach` recovery improved capability from `5/15` to `10/15` channels by wiring `mcporter`, `xreach`/`xreach-cli`, Exa MCP, Xiaohongshu MCP, Weibo MCP, plus container-backed `gh` and `ffmpeg` wrappers inside `/home/gaga/agent-reach-env`.
- GitHub access is available through the host-side `agent-reach` wrapper path because the backing container already has a valid logged-in `gh` session; a true host-local `gh` install was not required.
- Remaining `agent-reach` gaps after the safe recovery pass are mainly external prerequisites: proxy for Reddit/V2EX, Groq API key for podcast transcription, and future Douyin/LinkedIn MCP setup.
- OpenClaw runtime stayed healthy during this work; no host self-repair (`openclaw-health-check`, restart, rebuild) was needed.
- Completed the scoped `English-study / Label Studio` docs-and-identity cleanup pass in `projects/english-study/repo` for `README.md`, `pyproject.toml`, `docs/source/guide/project_settings.md`, and `docs/source/guide/project_settings_lse.md`; kept ML/model integration wording intact while fixing fork identity and doc phrasing.
- Initialized `.learnings/` with `ERRORS.md`, `LEARNINGS.md`, and `FEATURE_REQUESTS.md`, so the `self-improvement` skill can log learnings directly.
- Verified `tavily` end-to-end with the new `TAVILY_API_KEY` by running the bundled `skills/tavily-search/scripts/search.mjs`; live search returned results successfully.
- Checked the current workspace and both likely OpenClaw skill roots (`/home/gaga/.openclaw`, `/home/node/.openclaw`) for `find-skill`; no runtime-visible files or directories exist, so it should be treated as not installed.
- Aligned `SECURITY_TODO.md` with the live queue by recording `tavily` verification as complete and treating `find-skill` as unavailable unless it shows up in an active skill root later.
