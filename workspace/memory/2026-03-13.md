# 2026-03-13

- Heartbeat started; continued P1 work on the Feishu todo-capture flow and checked queue/cron state.
- Synced tracking docs: confirmed no child agents were active, then marked the multi-project convention item done in `SECURITY_TODO.md` because `MULTI_PROJECT_CONVENTION.md` is already defined and indexed.
- Simplified the multi-agent control setup after review: kept `main` ownership and differentiated worker roles, but trimmed repeated rules, rigid timing language, and noisy reporting requirements across `TEAM_ROLES.md`, `MAIN_CONTROL_POLICY.md`, `HEARTBEAT.md`, `AGENTS.md`, `SOUL.md`, and `MEMORY.md`.
- User preference: after the worker setup is stable, rename the subagents to simple Chinese role names so future progress updates are easier to discuss.
- User rule: when code changes are worth keeping, create meaningful rollback-value git commits at least once per day, avoid committing every tiny edit, and always tell the user clearly what changed.
- User preference: when asked to create an agent, validate it and then bring it up as a real active/visible agent session instead of stopping at file-based setup.
- User preference: report times in Beijing time (UTC+8) by default instead of UTC.
- User clarified that current multi-agent work is not considered successful unless agents are truly active and each role is visibly doing its own job, not just defined in files.
- Current long-running agent lineup the user wants to stabilize: `主工`, `验工`, `督工`, `参谋`, `谋财`, and `交付官`.
- New long-running project lane: `交付官` should own software projects near handoff, starting with `English_study` (a Label Studio fork for English-learning use), focusing first on read-only code scanning, issue finding, future model/backend integration, testing, polish, and delivery documentation.
- User wants the monetization role to be a standing role that keeps thinking about realistic赚钱方向, records durable lessons, and can continue across projects.
- User feedback corrected two important beliefs: (1) the Feishu bottom menu currently only sends plain text `记录待办` and is not yet a real functional flow, and (2) the repo clone blocker was SSH-vs-HTTPS mismatch rather than general GitHub auth failure.
- User explicitly does not want one Feishu bot per internal agent right now; keep one main user-facing entry and treat internal agents as background workers unless a future role needs its own dedicated external surface.
- Current OpenClaw channel/runtime limitation discovered: this environment does not support thread-bound persistent subagent sessions for visible long-running workers, so `sessions_spawn` can run parallel worker tasks in `mode: run` but cannot yet create the exact UI-visible persistent worker threads the user expects.
- User asked for Playwright capability on the `English_study` / Label Studio fork: wire up MCP/Playwright, run one real browser pass, and then report the major issues that need fixing.
- Clarified ownership split: `参谋` may do general research, but long-running monetization work should live under `谋财`; earlier monetization artifacts originated from `参谋` and were later handed off, which created temporary overlap.
- User rule: when a worker hits a permission issue or another blocker it cannot resolve safely, it should report upward to main first; main then decides what to surface to the user instead of letting the worker silently stall.
