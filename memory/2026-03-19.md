## 2026-03-19 16:22 UTC

- User confirmed the canonical runtime restart path on this machine is `cd ~/openclaw && ./scripts/start-local-runtime.sh`.
- Do not tell the user to use bare `docker compose up -d --force-recreate` for this stack; it can miss the required compose-file combination. Treat `scripts/start-local-runtime.sh` as the source-of-truth startup entrypoint.
- User explicitly asked that `.env` should not be read directly for GitHub token handling.
- User preference reinforced: all proactive progress pushes must be very short Chinese, and all user-facing times must be Beijing time (UTC+8).
- GitHub PAT setup remains unresolved. Current live checks show `gh auth status` reports `The token in GITHUB_TOKEN is invalid`, and `gh api user` returns `401 Bad credentials`.
- GitHub repo targets confirmed by the user: `https://github.com/lxsdsd/openclaw.git` for OpenClaw config/workspace migration content, and `https://github.com/lxsdsd/English_study.git` for the English-study project.
- Runtime/browser state checked today: OpenClaw browser profile `openclaw` expects CDP on port `18800`; extension relay profile `chrome-relay` uses port `18792`; host browser auto-start currently fails with `No supported browser found`, and profile `user` attach currently fails with `BrowserProfileUnavailableError` / `Connection closed`.
- Memory system state verified today: `openclaw memory status --agent main --json` reported backend `qmd`, workspace `/home/gaga/openclaw/var/workspace`, db path `/home/node/.openclaw/agents/main/qmd/xdg-cache/qmd/index.sqlite`, sources `memory` + `sessions`, files `14`, chunks `14`, vector available `true`, dirty `false`.
- Hooks state verified today: `openclaw hooks list --verbose` showed `session-memory` and workspace hook `self-improvement-reminder` as ready.
- Skill/runtime verification summary: `agent-reach`, `github`, `gh-issues`, `self-improvement`, `skill-vetting`, `tavily`, `summarize`, `mcporter`, `weather`, and `session-logs` were reported ready.
- Agent-reach / MCP verification today: Xiaohongshu MCP login is valid (`用户名: xiaohongshu-mcp`), Xiaohongshu list/search returned live results, Weibo hot/trending and content search returned live results, and `agent-reach doctor` reported `11/15` channels available. Not-yet-configured channels called out by doctor include Douyin and LinkedIn.
- Security/runtime blockers still unresolved from today's checks:
  - `/home/node/.openclaw/credentials` mode is `777` and should be `700`.
  - `channels.feishu.groupPolicy="open"` with elevated/runtime/fs tools remains a critical security finding.
  - Gateway token is still considered short.
  - No gateway auth rate limit is configured.
  - `openclaw security audit --deep` continues to report `missing scope: operator.read` for gateway probing.
- Reporting blocker still unresolved: `report-2200-bjt` is the late-night Beijing report job that keeps timing out; `report-2000-bjt` remains the healthy reference path. The remaining blocker is that this session still cannot inspect the live cron definition cleanly because the current gateway path lacks `operator.read`, and Docker CLI is not available in this runtime.
- Repo/worktree note: `/home/gaga/openclaw/var/workspace/assistant-state-backup/.git/config` now has remote `origin = https://github.com/lxsdsd/openclaw.git`. `/home/gaga/openclaw/var/workspace/projects/english-study/repo/.git/config` already points `origin` to `https://github.com/lxsdsd/English_study.git`, but that repo metadata looks incomplete/broken for normal git operations (for example `.git/HEAD` is missing), so push readiness there still needs repair.

## 2026-03-19 17:08 UTC

- GitHub PAT troubleshooting update: after the user regenerated a classic PAT and restarted with `cd ~/openclaw && ./scripts/start-local-runtime.sh`, live checks still show `gh auth status` -> `The token in GITHUB_TOKEN is invalid` and `gh api user` -> `401 Bad credentials`.
- Current agent runtime environment confirms `GITHUB_TOKEN` is injected into the running process: present `yes`, length `40`, prefix kind `ghp_`, no surrounding quotes, no spaces, no tabs, no `\r`, no `\n`, fingerprint prefix `ec45395d5b8d`. `GH_TOKEN` is absent, so there is no same-process override from `GH_TOKEN`.
- Current session limitation: this runtime still cannot directly inspect the host-side `~/openclaw/.env` or use Docker CLI, so it cannot yet prove whether the running `GITHUB_TOKEN` exactly matches the newest host `.env` value. In this session `/home/gaga/openclaw` only exposes the `var/` subtree, and `docker` is unavailable.
- Working hypothesis refined: GitHub token injection into the current process succeeds, but either (a) the injected token itself is invalid to GitHub, or (b) the runtime is still consuming an older value from a different state/root path. The next safe check should compare host `.env` vs running container token by fingerprint/length only, without printing token contents.
- GitHub token status resolved later in the session: direct GitHub API validation returned `HTTP 200` with login `lxsdsd`, and `gh auth status` updated to `Logged in to github.com account lxsdsd (GITHUB_TOKEN)` with scopes `admin:org`, `repo`, `workflow`. Repo access checks succeeded for both `lxsdsd/openclaw` (`viewerPermission: ADMIN`, `visibility: PUBLIC`) and `lxsdsd/English_study` (`viewerPermission: ADMIN`, `visibility: PRIVATE`).
- Root cause of the earlier GitHub auth failures: `~/.profile` had a stale hardcoded `GITHUB_TOKEN` that overrode the `.env`-provided token; after removing that override and restarting via `cd ~/openclaw && ./scripts/start-local-runtime.sh`, the live runtime fingerprint changed to `267f3ca8345a` and GitHub auth recovered.
- Git credential chain now works through `gh`: current `git push --dry-run` for `/home/gaga/openclaw/var/workspace/assistant-state-backup` reaches `https://github.com/lxsdsd/openclaw.git` and fails only with non-fast-forward (`fetch first`), which confirms the remaining blocker is branch divergence rather than auth.
- User requested a durable repo split rule going forward: separate OpenClaw migratable config/workspace content from the English-study project, then push the relevant historical git commits to the matching cloud repos instead of keeping both histories mixed together.
- Current split finding: the OpenClaw-side snapshot repo `assistant-state-backup` already contains tracked `workspace/projects/english-study/repo/...` history, while the standalone `projects/english-study/repo/.git` is broken and only retains `config`, so earlier English-study history likely needs to be recovered/extracted from the OpenClaw-side snapshot history.
