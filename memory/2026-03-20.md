# 2026-03-20

- 01:42 UTC: User confirmed the local `assistant-state-backup/config/openclaw.json` tool-allowlist change should be kept. Do not treat that edit as disposable drift during repo cleanup.
- 01:42 UTC: User also set a standing rule that each active project should have its own corresponding repo; when appropriate, create the repo instead of leaving projects mixed together.
- 02:05 UTC: User provided the formal Feishu task Bitable URL `https://fcn8b7t5ltdk.feishu.cn/wiki/BcDgwPnOqi55LXkt7WpclPZyn5c?table=tblMVX0NKSoy3Oyb&view=vewXxBNTOK`. Durable IDs: `app_token=P1B0bfR5saT9bys80GPcMr8rnkf`, `table_id=tblMVX0NKSoy3Oyb`, table name `✅任务管理`.
- 02:05 UTC: Current synced task record IDs in that Bitable: `recvemXt5gh3HM` = fix local `operator.read` read path; `recvemXt6GJ2E3` = reconcile `report-2000-bjt` / `report-2200-bjt` scheduler truth; `recvemXt7xfDsm` = finish OpenClaw / `English_study` repo-boundary and default-branch cleanup; `recFIE3n52` = openclaw.json permission hardening lane marked completed; `recvedsEZj4w5s` = `skills/tavily-search` review/disposition decision.
- 02:05 UTC: Durable blocker facts: live cron store file `/home/node/.openclaw/cron/jobs.json` currently shows `jobs: 0`; board notes about `report-2000-bjt` / `report-2200-bjt` are therefore stale or point to a moved scheduler source. Local live-read problem remains the CLI `operator.read` credential-selection path, not pairing loss.
- 02:05 UTC: Runtime/tooling blocker: workspace helper `scripts/openclaw-live-cli.sh` is the right container-truth path but cannot run from the current agent runtime because no usable `docker` binary is available here.
- 02:05 UTC: `skills/tavily-search` manual review result: only files are `SKILL.md`, `scripts/search.mjs`, and `scripts/extract.mjs`; scripts read `TAVILY_API_KEY` and send it plus user-supplied query/URL only to Tavily official endpoints `https://api.tavily.com/search` and `https://api.tavily.com/extract`. Treat as reviewed remote-API usage still flagged by audit, not confirmed broad secret harvesting.

## Late session (03:00–05:00 UTC)

- 03:03 UTC: Subagent `dist-auth-inspector` completed. Confirmed via `/app/dist` code review that `gateway status`, `gateway probe`, `gateway call`, and `status --all` use genuinely different auth/connection code paths. Not flaky — architectural divergence.
- 03:44 UTC: User confirmed the root cause analysis is correct and provided additional local evidence:
  - Source files exist in the local source tree: `src/gateway/operator-device-auth.ts`, `src/gateway/call.ts`, `src/gateway/probe-auth.ts`, `src/commands/gateway-status/helpers.ts`.
  - Commit `c600b340d` contains the operator auth unification fix.
  - But live container `/app/dist` still has old logic.
- 03:44 UTC: User's conclusion: fix direction is correct, source is done, but running container hasn't been rebuilt/redeployed.
- 04:14 UTC: Repo boundary cleanup confirmed complete. Cloud state: `lxsdsd/openclaw` default branch = `workspace-main`, `lxsdsd/English_study` default branch = `main`.
- 04:55 UTC: Agent confirmed execution blocker: current agent runtime only exposes `/app/dist`. Rebuild/redeploy must be done from host-side source worktree.

## Plugin allowlist rule (11:08 UTC)

- `plugins.allow` in `openclaw.json` is an explicit allowlist. Schema: "when set, only listed plugins are eligible to load."
- Standing rule: **every time a new plugin is added, its ID must also be added to `plugins.allow`**, otherwise it won't load.
- User confirmed Codex installed two plugins (`context-safe`, `openclaw-customprovider-cache`) and updated config including model fallbacks and allowlist. Commit `8845cd12d` on workspace-main.

## Full allowlist expansion (11:12 UTC)

- Per user instruction, expanded `plugins.allow` from 3 entries to all 44 valid bundled + custom plugins.
- Applied via `gateway config.patch`, restart triggered (SIGUSR1).

## C盘空间清理 (11:18 UTC)

- C盘 201G 已用 198G，仅剩 2.7G（99%满）
- 最大元凶：`docker_data.vhdx` = 68G（WSL2 + Docker Desktop 虚拟磁盘只增不缩）
- 已删除：Windows Temp wsl-crashes (5.2G)、assistant-state-backup (520M)、openclaw_recovery (19M)、var/backups (23M)、agent-reach-env (531M)、Docker dangling volumes
- 清理后 C 盘剩余：7.8G（从 2.7G 恢复到 7.8G，回收约 5.1G）
- 飞书清单文档：https://feishu.cn/docx/XukodpNBtoKrbTxjdGAcqAuYn5b
- 验证结果：OpenClaw gateway healthy，所有 workspace/config/memory 文件完整
- 长期规则：不在 C 盘做本地备份，改用 git 云端 + D 盘；定期 compact docker_data.vhdx
- TODO_USER: docker_data.vhdx compact 需要管理员 PowerShell 手动执行

## disk-cleanup skill 创建 + context-safe 更新 (12:15 UTC)

- context-safe 插件从 v0.3.0 更新到 v0.4.1 (commit `08da1bf`)，同步到 `var/config/extensions/context-safe/`，已重启生效
- 创建了 `skills/disk-cleanup/` skill：SKILL.md（30+ 保护路径、三级清理、工作流、紧急流程）、scan-disk.sh（只读扫描）、最佳实践参考
- 已上传到 GitHub: `lxsdsd/openclaw-disk-cleanup-skill`
- 设置了每周 cron job: `disk-cleanup:space-check`, id=`c7920771-699d-415c-8499-cde3b47063c0`, 每周一 02:00 UTC (10:00 BJT)

## memory 文件重复问题修复 (12:23 UTC)

- 发现 memory/2026-03-20.md 文件内容被完整重复了一遍（两个 `# 2026-03-20` header），导致 Edit 工具匹配失败报错
- 根因：之前多次 Edit 操作时，oldText 匹配到了重复段落的第一个而 newText 未能正确替换，加上中间一次 Write 未清理干净
- 已用 Write 完整重写为去重版本

## Git proxy fix + operator.read 深层根因 (13:21 UTC)

- 用户执行了 `git config --global http.proxy http://host.docker.internal:7897`，但 `host.docker.internal` 在 WSL 里解析到 `192.168.1.62`（局域网 IP），不是代理。已修正为 `172.22.144.1:7897`，git fetch 通了。
- operator.read 根因进一步确认：`Dockerfile.agent-reach` 第一行是 `FROM ghcr.io/openclaw/openclaw:latest`，所以 `--build` 只会重建 agent-reach 叠加层，不会用到本地 `src/` 的修复。需要先用主 Dockerfile 构建本地基座 `openclaw:local`，再改 `Dockerfile.agent-reach` 的 FROM。
- disk-cleanup skill 已补充 vhdx 压缩手动流程，已推到 GitHub (commit `26cac39`)。
- WSL git user config 设为 `lxsdsd` / `lxsdsd@users.noreply.github.com`。

## 飞书任务管理更新 (12:43 UTC)

- 新增 7 条任务到 Bitable:
  - `recvepxZdedgC4` = 多 Agent 并行执行验证与优化 (main, P2 重要不紧急)
  - `recvepxZcSmI9l` = 补配 BRAVE_API_KEY (用户, P2 重要不紧急)
  - `recvepxZcIujLi` = 给 WSL git 配置代理 → **已完成** (proxy 修正为 172.22.144.1:7897)
  - `recvepxZjtc01m` = Compact docker_data.vhdx (用户, P0 重要紧急)
  - `recvepxZdi298Y` = Rebuild + redeploy 含 operator auth fix 的容器 (用户, P0 重要紧急, 进行中)
  - `recvepxZcHNjpR` = 安全审计收紧 agent tool profile (main, P2 重要不紧急)
  - `recvepxZcLYCfm` = 重建定时报告 cron job (main, P3 不紧急不重要)

## operator.read rebuild 验证 (13:31 UTC)

- 用户说已做完 vhdx compact + rebuild，但验证发现修复**仍未生效**
- 镜像时间仍是 `2026-03-20T13:04:10`（之前那次），`Dockerfile.agent-reach` 第一行仍是 `FROM ghcr.io/openclaw/openclaw:latest`
- 容器 dist 里搜不到 `resolveStoredOperatorDeviceToken` / `preferStoredOperatorDeviceToken`，`disableDeviceIdentity` 仍在 3 个文件里
- 正确的修复路径：(1) 用主 Dockerfile 构建本地基座 `docker build -t openclaw:local .`，(2) 改 `Dockerfile.agent-reach` 的 FROM 为 `openclaw:local`，(3) 再 `--build`
- 等待用户确认是否由 agent 帮改 Dockerfile.agent-reach

## operator.read 修复最终验证通过 (14:37 UTC)

- 用户 + Codex 补完了 `start-local-runtime.sh --build` 逻辑：现在 `--build` 会先 `docker build -t openclaw:local .` 构建基座，再 compose build overlay
- 用户执行了完整 rebuild，基座镜像 `openclaw:local` 构建于 14:23 UTC，容器 `openclaw-agent-reach:local` 构建于 14:33 UTC
- 验证结果：`resolveStoredOperatorDeviceToken` 在 33 个 dist 文件中命中，`disableDeviceIdentity` 完全消失
- `gateway probe`: Reachable: yes (27ms)
- `status --all`: 无 `missing scope` / `operator.read` 报错
- 宿主机 CLI 需要 `OPENCLAW_GATEWAY_TOKEN` + `OPENCLAW_GATEWAY_PORT=18789` 环境变量才能连容器 gateway
- 飞书任务 `recvepxZdi298Y` 已标记完成

## 用户操作指令 (13:29 UTC)

- 用户要求按任务管理重要级别开始处理待办，遇到需要用户插手的告诉用户后继续下一项，直到全部处理完
- 用户要求 `start-local-runtime.sh` 加 `--build` 参数（由 Codex 完成）
- 用户要求 Codex 完成的修改记住而不需要每次提醒
- 用户确认 vhdx compact 和 rebuild 均已完成（但 Dockerfile.agent-reach FROM 未改）

## Durable IDs

- Feishu task Bitable: `app_token=P1B0bfR5saT9bys80GPcMr8rnkf`, `table_id=tblMVX0NKSoy3Oyb`
- operator.read task record: `recvemXt5gh3HM` (original) + `recvepxZdi298Y` (new detailed)
- Source commit with fix: `c600b340d`
- Latest workspace push: `cda691c34` → `origin/workspace-main`
- GitHub repos: `lxsdsd/openclaw` (default: `workspace-main`), `lxsdsd/English_study` (default: `main`), `lxsdsd/openclaw-disk-cleanup-skill` (public)
- Cron jobs: `disk-cleanup:space-check` = `c7920771-699d-415c-8499-cde3b47063c0`
- disk-cleanup skill GitHub commit: `26cac39` (vhdx compact procedure added)
- WSL git proxy: `http://172.22.144.1:7897` (correct address, `host.docker.internal` resolves to LAN IP not proxy)

## 飞书投递丢消息修复 (01:55 北京时间)

- 根因：streaming card 启动失败时无 fallback，消息被静默丢弃
- 修复：关闭 `channels.feishu.streaming = false`
- "400 Duplicate item" 是 OpenClaw 框架 bug，长会话 history 组装时产生重复 content block id，配置层无法修复
- tools.deny 配置已确认全部生效
