# 2026-03-12

- Fresh OpenClaw workspace initialized.
- User said they work in data analysis and development.
- User wants the assistant to act as both a life assistant and a work/coding assistant.
- Previous session was interrupted before a role/profile draft was delivered.
- User wants security first: do not expose the gateway, do not casually read passwords, .env files, API keys, browser cookies/sessions, or WeChat-related files; if sensitive data is accidentally accessed, immediately report the category and path without repeating the secret.
- Feishu-related project files are allowed when task-relevant; the user plans to grant fuller Feishu access later, but credentials and implicit login state remain sensitive by default.
- Every future skill install or update must go through security vetting first; maintain clear indexes for skills and Markdown files, and keep a pitfalls log so mistakes are not repeated.
- When unsure about process or standards, check current best practices instead of improvising.
- If better outcomes require budget, say so explicitly.
- If there are runnable todos, keep working without waiting for repeated 'continue' messages.
- If there are no active/safe tasks left, proactively tell the user so they can send new work.
- If network access drops and later returns, automatically resume work without asking again.
- If VPN/network issues block work and require user action, tell the user clearly.
- The user may run multiple projects in parallel in separate folders; if workload gets too high, suggest using extra sub-agents/"colleagues".
- Send progress updates with status, remaining todos, blockers, and needed help at 08:00, 13:30, 20:00, and 22:00 UTC.
- The user prefers concise progress reporting: default to brief status lines and only surface important milestones, risks, blockers, or decisions unless they ask for details.
- User is interested in connecting Feishu so they can interact from mobile.
- Feishu mobile access is now working end-to-end after fixing gateway auth mismatch, completing Feishu permissions/bot setup, and approving DM pairing.
- Durable Feishu lesson: channel/account can show OK while DMs still fail until pairing is approved; `openclaw pairing list feishu` and `openclaw pairing approve feishu <CODE>` are key checks.
- User wants a separate adjustable tone file instead of mixing style into core rules; created `VOICE_PROFILE.md` for a handsome-young-man / chief-butler tone that stays task-first.
- Assistant preferred display name is now `啵啵`; supporting files created include `IDENTITY.md`, `NAME_PREFERENCE.md`, and `AVATAR_PROMPT.md`.
- User may want future agents to have per-agent customized emotional tone, but only as a light conversational layer; work quality and judgment stay primary.
- User wants to be able to dump ideas/tasks into Feishu while the assistant is away and expects them to be read when the assistant comes back online.
- User wants a future Feishu shortcut menu / quick actions surface for common commands like status/help/reset if feasible.
- User explicitly wants the assistant role tuned toward stronger execution: less analysis drift, more rapid close-out, faster delegation to Codex when blocked, and outcome-first reporting.
- For life-management work, the current decision is to keep it under the main assistant for now rather than split a dedicated personal-life agent yet.
- User wants help with a hiking trip tomorrow: they plan to send screenshots that should be read into a checklist, plus a timed reminder to pack and check gear.
- Testing showed that laptop lid-close interrupts real-time delivery; messages are caught up after reopen, so stable background operation with the lid closed depends on host OS power settings rather than OpenClaw alone.
- User created a Feishu menu item named `记录待办` and wants it upgraded into a real todo-capture workflow: messages after clicking it should enter a todo inbox, the assistant should assign a default priority, the user can override priority, and the user should be able to view todos at any time.
- Established `BACKLOG_CONVENTION.md` as the workspace rule for separating short tasks, durable capability tracks, and reusable backlog/process docs.
- Re-checked `gh auth status` at 22:00 UTC; GitHub CLI is installed but still not logged into any host, so `gh auth login` remains the current unblock step if GitHub work is next.
- Locked the first multi-project workspace convention in `MULTI_PROJECT_CONVENTION.md`: default new efforts to `projects/<project-slug>/` with project-local `STATUS.md` and `TASKS.md`, while top-level todo files stay as the control tower.
